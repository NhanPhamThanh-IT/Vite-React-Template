/**
 * Custom Hooks Module Index
 * 
 * @module hooks/index
 * @description Barrel file that exports all custom hooks for easier importing
 */
import useLocalStorage from './useLocalStorage'
import useForm from './useForm'

export {
  useLocalStorage,
  useForm
}
